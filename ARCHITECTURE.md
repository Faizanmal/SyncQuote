# SyncQuote - System Architecture

This document provides a comprehensive overview of the SyncQuote system architecture, including data flows, module dependencies, security layers, and deployment patterns.

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (Next.js 14)                    │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │  Dashboard      │  │  Proposals      │  │  Analytics      │ │
│  │  - Overview     │  │  - List         │  │  - Metrics      │ │
│  │  - Quick Stats  │  │  - Detail       │  │  - Funnel       │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │  Templates      │  │  Settings       │  │  Notifications  │ │
│  │  - Gallery      │  │  - Branding     │  │  - Real-time    │ │
│  │  - Create       │  │  - Profile      │  │  - Bell         │ │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘ │
├─────────────────────────────────────────────────────────────────┤
│  Components: notification-bell.tsx • analytics-dashboard.tsx   │
│             template-gallery.tsx • comment-thread.tsx           │
└───────────────────────┬─────────────────────────────────────────┘
                        │
                        │ HTTP/REST + WebSocket
                        │
┌───────────────────────▼─────────────────────────────────────────┐
│                      BACKEND (NestJS)                            │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────────┐│
│  │                   API MODULES                                ││
│  ├─────────────────────────────────────────────────────────────┤│
│  │ Core: Auth • Users • Proposals • Comments • Events • Email  ││
│  │ New:  Templates • Notifications • Analytics • Audit         ││
│  └─────────────────────────────────────────────────────────────┘│
│  ┌─────────────────────────────────────────────────────────────┐│
│  │                REAL-TIME GATEWAY                             ││
│  ├─────────────────────────────────────────────────────────────┤│
│  │  EventsGateway (Socket.io)                                  ││
│  │  • proposal:viewed • comment:added • notification:new       ││
│  └─────────────────────────────────────────────────────────────┘│
└───────────────────────┬─────────────────────────────────────────┘
                        │
                        │ Prisma ORM
                        │
┌───────────────────────▼─────────────────────────────────────────┐
│                   DATABASE (PostgreSQL)                          │
├─────────────────────────────────────────────────────────────────┤
│  Core Tables: User • Proposal • Comment • Activity              │
│  New Tables:  Template • Notification • AuditLog                │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Key Data Flows

### Real-Time Notification Flow
```
Client Views Proposal → Backend Tracks View → Creates Notification → WebSocket Alert → Owner Notified
```

### Template to Proposal Flow
```
User Selects Template → Service Gets Content → Creates New Proposal → Copies Blocks → Editor Opens
```

### Analytics Pipeline
```
User Actions (view/comment/sign) → Activity Table → Analytics Service → Dashboard Display
```

## 🗂️ Module Dependencies

```
AppModule
├── AuthModule → UsersModule → PrismaModule
├── ProposalsModule → [PrismaModule, EventsModule, StorageModule]
├── TemplatesModule → PrismaModule ✨
├── NotificationsModule → [PrismaModule, EventsModule, EmailModule] ✨
├── AnalyticsModule → PrismaModule ✨
├── AuditModule → PrismaModule ✨
├── CommentsModule → [PrismaModule, EventsModule] (enhanced)
└── EventsModule → [PrismaModule, EmailModule] (enhanced)
```

---

## 📊 Database Schema Relationships

```
User ────────┬──────────┐
             │          │
             │          │
    ┌────────▼───┐  ┌───▼──────┐
    │ Proposal   │  │ Template │ ✨
    │            │  │          │
    │ • title    │  │ • name   │
    │ • status   │  │ • category│
    │ • slug     │  │ • content│
    └────┬───────┘  └──────────┘
         │
         ├──────┬──────────┬───────────┐
         │      │          │           │
    ┌────▼───┐ │     ┌────▼──────┐ ┌──▼────────┐
    │ Block  │ │     │ Comment   │ │ Activity  │
    └────────┘ │     │ (threaded)│ └───────────┘
               │     │ • parentId│ ✨
               │     └───────────┘
               │
          ┌────▼──────────┐
          │ PricingItem   │
          └───────────────┘

User ────────┬──────────┬───────────┐
             │          │           │
        ┌────▼──────┐ ┌─▼────────┐ │
        │Notification│ │ AuditLog │ │ ✨
        │           │ │          │ │
        │ • type    │ │ • action │ │
        │ • read    │ │ • changes│ │
        └───────────┘ └──────────┘ │
                                   │
                              ┌────▼──────┐
                              │ Template  │ ✨
                              └───────────┘
```

---

## 🎯 API Endpoint Map

```
/api
├── /auth
│   ├── POST   /signup
│   ├── POST   /login
│   ├── POST   /logout
│   └── POST   /refresh
│
├── /users
│   ├── GET    /profile
│   ├── PATCH  /profile
│   └── GET    /me
│
├── /proposals
│   ├── GET    /
│   ├── POST   /
│   ├── GET    /:id
│   ├── PATCH  /:id
│   └── DELETE /:id
│
├── /templates ✨
│   ├── GET    /
│   ├── POST   /
│   ├── GET    /:id
│   ├── PATCH  /:id
│   ├── DELETE /:id
│   └── POST   /:id/use
│
├── /notifications ✨
│   ├── GET    /
│   ├── GET    /unread-count
│   ├── PATCH  /:id/read
│   └── POST   /mark-all-read
│
├── /analytics ✨
│   ├── GET    /overview
│   ├── GET    /engagement
│   ├── GET    /funnel
│   └── GET    /activity
│
├── /audit ✨
│   ├── GET    /logs
│   ├── GET    /proposal/:id
│   └── GET    /export
│
└── /comments
    ├── GET    /
    ├── POST   /
    └── PATCH  /:id/resolve ✨
```

---

## 🔐 Security Layers

```
┌─────────────────────────────────────────┐
│         Client Request                   │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│   Layer 1: Rate Limiting                │
│   • 100 requests per minute             │
│   • 5 auth attempts per minute          │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│   Layer 2: CORS                         │
│   • Allowed origins                     │
│   • Credentials enabled                 │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│   Layer 3: JWT Authentication           │
│   • Token validation                    │
│   • User identification                 │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│   Layer 4: Authorization                │
│   • Resource ownership check            │
│   • Role-based access                   │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│   Layer 5: Audit Logging ✨            │
│   • IP address                          │
│   • User agent                          │
│   • Action tracking                     │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│         Controller Handler              │
└─────────────────────────────────────────┘
```

---

## 📈 Performance Optimization Points

```
Frontend
├── Code Splitting
│   ├── Route-based
│   ├── Component lazy loading
│   └── Dynamic imports
│
├── Caching
│   ├── React Query
│   ├── Local storage
│   └── Service worker (future)
│
└── Optimization
    ├── Image optimization (Next.js)
    ├── Bundle size reduction
    └── Tree shaking

Backend
├── Database
│   ├── Indexes on key fields ✅
│   ├── Query optimization
│   └── Connection pooling
│
├── Caching (Redis)
│   ├── Analytics results
│   ├── Template lists
│   └── User sessions
│
└── API
    ├── Pagination
    ├── Field selection
    └── Compression
```

---

## 🔄 Real-Time Event Flow

```
WebSocket Events:

User Connects
    │
    ├─► join_user (userId)
    │   └─► Server: Add to user:{userId} room
    │
    ├─► join_proposal (proposalId)
    │   └─► Server: Add to proposal:{proposalId} room
    │
    └─► Server Events:
        │
        ├─► notification (new notification) ✨
        │   └─► Emitted to: user:{userId}
        │
        ├─► comment_added (new comment)
        │   └─► Emitted to: proposal:{proposalId}
        │
        ├─► proposal_viewed (view event)
        │   └─► Emitted to: user:{ownerId}
        │
        └─► proposal_approved (signature)
            └─► Emitted to: user:{ownerId}
```

---

## 🎨 Component Hierarchy

```
App
├── Providers
│   ├── AuthProvider
│   ├── QueryClientProvider
│   └── WebSocketProvider ✨
│
├── Layout
│   ├── Header
│   │   ├── Logo
│   │   ├── Navigation
│   │   └── NotificationBell ✨
│   │
│   └── Sidebar
│       ├── DashboardLink
│       ├── ProposalsLink
│       ├── TemplatesLink ✨
│       ├── AnalyticsLink ✨
│       └── SettingsLink
│
└── Pages
    ├── Dashboard
    │   ├── Overview
    │   └── QuickStats
    │
    ├── Proposals
    │   ├── ProposalList
    │   └── ProposalDetail
    │       └── CommentThread ✨
    │
    ├── Templates ✨
    │   └── TemplateGallery ✨
    │
    ├── Analytics ✨
    │   └── AnalyticsDashboard ✨
    │
    └── Settings
        ├── Profile
        └── BrandingSettings ✨
```

---

## 🚀 Deployment Architecture

```
Production Environment:

┌─────────────────────────────────────────┐
│         Vercel (Frontend)                │
│         • Next.js SSR                    │
│         • Edge Functions                 │
│         • CDN                            │
└──────────────┬──────────────────────────┘
               │
               │ HTTPS
               │
┌──────────────▼──────────────────────────┐
│      AWS ECS (Backend)                  │
│      • Docker Container                  │
│      • Auto-scaling                      │
│      • Load Balancer                     │
└──────────────┬──────────────────────────┘
               │
               ├─────────────┬──────────────┐
               │             │              │
┌──────────────▼──┐  ┌───────▼──────┐  ┌──▼────────┐
│ AWS RDS         │  │ ElastiCache  │  │ S3        │
│ (PostgreSQL)    │  │ (Redis)      │  │ (Storage) │
└─────────────────┘  └──────────────┘  └───────────┘
```

---

## 📊 Monitoring Stack

```
Observability:

┌─────────────────────────────────────────┐
│          Application                     │
└──────────────┬──────────────────────────┘
               │
               ├────────────┬──────────────┐
               │            │              │
┌──────────────▼──┐  ┌──────▼─────┐  ┌───▼───────┐
│ Sentry          │  │ Pino       │  │ Analytics │
│ (Errors)        │  │ (Logs)     │  │ (Metrics) │
└─────────────────┘  └────────────┘  └───────────┘
```

---

This architecture diagram provides a visual overview of how all the components work together in the SyncQuote system! 🎯
